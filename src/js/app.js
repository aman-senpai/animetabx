import { App } from './components/App.js';
import { AnimeQuote } from './animeQuote.js';

// Initialize the application
const app = new App();
app.init();

// Initialize anime quote feature
const animeQuote = new AnimeQuote(); 